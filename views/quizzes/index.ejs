<table>
  <% for (var i in quizzes) { %>        
     <tr>
        <td>
           <a href="/quizzes/<%= quizzes[i].id %>"><%= quizzes[i].id %><%= quizzes[i].question %></a>
        </td>
     </tr> 
  <% } %>
</table>
<p>
<a href="/quizzes/new"><button type="button">Crear pregunta</button></a>

<form method="get" action="/quizzes">        
    <p>
      Buscar <%= search %>
    </p>

    <p>
      <input type="text" name="search" value="<%= search %>" placeholder="Respuesta" />

      <input type="submit" value="Comprobar">
    </p>
  </form>
  </p>
